# Explicitly specify compatible versions
streamlit==1.25.0
lottie==0.7.0
streamlit-lottie==0.0.5
pandas==1.5.3
seaborn==0.12.2
matplotlib==3.6.3
Pillow==9.4.0
numpy==1.24.3
setuptools==65.5.1  # This includes distutils needed for building certain packages

# Force binary-only installation for numpy to prevent build issues
numpy==1.24.3 --only-binary=:all:
